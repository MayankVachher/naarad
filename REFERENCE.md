# Reference

> Auto-generated by `scripts/gen_reference.py` from the pydantic config
> models and the command registry. Do not edit by hand — re-run the
> script instead.

## Configuration

Everything lives in `config.json` (gitignored). The example file
`config.example.json` shows the full shape with example values.

| Key | Default | Description |
|-----|---------|-------------|
| `telegram.token` | **required** | BotFather token. |
| `telegram.chat_id` | **required** | Your personal chat ID — the bot only talks to this chat. |
| `eodhd.api_key` | **required** | EODHD API key (real-time quotes + per-exchange holiday calendar). |
| `timezone` | `"America/Toronto"` | IANA timezone for water + morning schedules. |
| `water.active_end` | `"21:00"` | After this time-of-day, no reminders fire until next morning's start. |
| `water.intervals_minutes` | `[120, 60, 30, 15, 5]` | Escalation curve. The Nth value is the gap before reminder N+1 if you keep ignoring them. |
| `water.first_reminder_delay_minutes` | `3` | Grace between the morning brief's [Start day] tap and the first reminder. Welcome-button taps bypass it. |
| `water.daily_target_glasses` | `8` | Target glass count per day. Drives `/status` progress + pace-adjusted intervals. Set `0` to disable pace adjustment. |
| `water.pace_floor` | `0.3` | Minimum interval multiplier when behind pace. Caps the squeeze (default 0.3 = at most ~3× normal cadence). Range (0, 1]. |
| `brief.location_name` | `"Toronto"` | City name shown in the morning brief. |
| `brief.location_lat` | `43.6532` | Latitude for the weather + sunrise lookup. |
| `brief.location_lon` | `-79.3832` | Longitude for the weather + sunrise lookup. |
| `morning.start_time` | `"06:00"` | When the daily brief is generated and sent. |
| `morning.fallback_time` | `"11:00"` | If you haven't tapped [Start day] by this time, the water chain auto-starts. |
| `llm.enabled` | `true` | Compile-time floor for LLM features. `false` makes the `/llm` toggle inert. |
| `llm.backend` | `"copilot"` | Which CLI to shell out to: `"copilot"` or `"claude"`. |
| `tickers.enabled` | `true` | Compile-time floor for the market jobs + `/quote`. `false` makes the `/ticker on\|off` toggle inert. |
| `tickers.market_timezone` | `"America/New_York"` | Timezone the market_open/market_close jobs fire in; separate from user-facing `timezone`. |
| `tickers_default` | `[]` | Seed tickers planted into the DB on first init. Edits after first boot go through `/ticker`. |
| `schedules.market_open` | `"09:35"` | Market-open snapshot time in `tickers.market_timezone`. |
| `schedules.market_close` | `"16:05"` | Market-close snapshot time in `tickers.market_timezone`. |
| `db_path` | `"state.db"` | SQLite file path. |

## Commands

All commands are also surfaced in Telegram's `/` autocomplete menu
(top-level only) and printed by `/help` inside the bot.

| Command | Description |
|---------|-------------|
| `/water` | Confirm you drank water (resets the chain and bumps the day's glass count). |
| `/brief` | Re-run today's morning brief on demand. Useful for prompt iteration. |
| `/llm on\|off` | Toggle LLM-generated brief + water lines at runtime. |
| `/llm test` | Fire a one-shot prompt at the configured backend; reports ✓ + the model's line or ✗ + the reason. |
| `/llm backend [copilot\|claude]` | Show or change the LLM backend at runtime; overrides `llm.backend` until `state.db` is reset. |
| `/ticker add SYMBOL` | Track a new ticker (US bare, `.TO` suffix for TSX; symbol is validated). |
| `/ticker remove SYMBOL` | Stop tracking a ticker. |
| `/ticker list` | List tracked tickers. |
| `/ticker on\|off` | Runtime kill switch for market jobs + `/quote`. |
| `/quote SYMBOL` | On-demand real-time quote for a single symbol. |
| `/status` | Bot health, grouped into Water / LLM / Tickers / System sections. |
| `/help` | Command reference. |

You can also confirm water by tapping the **💧 Drank water** button on any reminder, or by replying to a reminder with anything.
